# Screenshots

Drop UI screenshots here so they render in the root README:

- `domain-tab.png` — a domain tab showing digest cards + trend banner
- `video-panel.png` — the Daily Video modal

Capture them from http://localhost:5173 while the app is running.
